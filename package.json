{
  "name": "tanstack-web3",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "dev:anvil": "anvil --host 0.0.0.0",
    "dev:web": "turbo dev --filter=@tanstack-web3/web",
    "dev:all": "bun run dev:anvil & turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "turbo format",
    "check": "turbo check"
  },
  "packageManager": "bun@1.3.8",
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
